% Suggested time: 1.4 minutes
\begin{frame}[t]{Credit assignment is the bottleneck in long traces}
  \begin{center}
    \begin{tikzpicture}
      \foreach \x in {0,...,11} {
        \pgfmathtruncatemacro{\bad}{ifthenelse(\x==4,1,0)}
        \ifnum\bad=1
          \node[draw=DeckRed,fill=DeckRed!16,minimum width=9mm,minimum height=8mm]
            (n\x) at (0.85*\x,0) {\scriptsize error};
        \else
          \node[draw=DeckMuted!70,fill=DeckSoft,minimum width=9mm,minimum height=8mm]
            (n\x) at (0.85*\x,0) {};
        \fi
      }
      \draw[decorate,decoration={brace,mirror,amplitude=5pt},DeckMuted]
        (-0.45,-0.55) -- (9.8,-0.55)
        node[midway,below=6pt] {\small 100,000 tokens of model calls, tools, and observations};
      \node[warnbox,text width=30mm] (reward) at (4.7,-1.9) {Final reward $\rightarrow A_i<0$};
    \end{tikzpicture}
  \end{center}

  \vspace{1mm}
  \small
  \begin{itemize}
    \item The outcome says \emph{the run failed}, but does not identify the responsible decision.
    \item GRPO gives the same negative advantage to the one bad action and every good action around it.
    \item More sibling rollouts improve the comparison; they do not explain the failure.
  \end{itemize}

  \vspace{1mm}
  \begin{center}
    \textcolor{DeckGold}{\textbf{Outcome}}
    $\longrightarrow$
    \textcolor{DeckBlue}{\textbf{responsible trace or step}}
    $\longrightarrow$
    \textcolor{DeckTeal}{\textbf{token-level update}}
  \end{center}
\end{frame}

% Suggested time: 1.5 minutes
\begin{frame}[t]{GRPO: let sibling attempts compete}
  \begin{center}
    \begin{tikzpicture}
      \node[signalbox,text width=24mm] (prompt) at (0,0) {One prompt};
      \node[flowbox] (a) at (-4.0,-1.25) {Attempt A\\\textcolor{DeckTeal}{reward 1}};
      \node[flowbox] (b) at (0,-1.25) {Attempt B\\\textcolor{DeckRed}{reward 0}};
      \node[flowbox] (c) at (4.0,-1.25) {Attempt C\\\textcolor{DeckTeal}{reward 1}};
      \node at (6.0,-1.25) {$\cdots$};
      \draw[flowarrow] (prompt.south) -- (a.north);
      \draw[flowarrow] (prompt.south) -- (b.north);
      \draw[flowarrow] (prompt.south) -- (c.north);
    \end{tikzpicture}
  \end{center}

  \vspace{2mm}
  \begin{columns}[T,onlytextwidth]
    \begin{column}{0.56\textwidth}
      \begin{enumerate}
        \item Sample a group from the current policy.
        \item Score each complete answer.
        \item Compare each reward with its group mean.
        \item Reinforce above-average attempts; suppress below-average attempts.
      \end{enumerate}
    \end{column}
    \begin{column}{0.40\textwidth}
      \[
        A_i=\frac{r_i-\bar r}{s_r+\epsilon}
      \]
      {\small Clipping limits reuse of stale rollouts; a reference KL limits destructive drift.}
    \end{column}
  \end{columns}

  \slidesource{DeepSeek-R1 and first-principles GRPO analyses \citep{guo2025deepseekr1,shen2026firstprinciplesgrpo}.}
\end{frame}

\subsection{Why outcome-only GRPO becomes insufficient}

GRPO improves reasoning when completed outcomes can be verified reliably, as demonstrated
by DeepSeek-R1 \citep{guo2025deepseekr1}. Its advantage nevertheless remains one number
for the whole trajectory. It cannot distinguish a decisive reasoning step from irrelevant
tokens, prompts with uniformly failed samples provide no relative signal, and imperfect
verifiers can reward shortcuts. Longer trajectories amplify these limitations while also
increasing sequential rollout cost.

Agent systems add a second problem: the optimized tokens must remain attached to the policy
version and exact context that generated them. Asynchronous workers create policy lag;
tool responses are context rather than policy actions; context replacement, subagents, and
parallel branches can require new trace boundaries rather than one fictional continuous
trajectory \citep{glm5team2026,xu2026polar}. These limits motivate the later comparison:
process reward models and critics attempt to localize progress, while on-policy
distillation supplies dense teacher feedback on the student's encountered states. They are
not merely alternative optimizers; they add information that terminal group comparison
does not contain.

Composer 2 provides a concrete compaction case. Its agent learns to emit self-summaries
during long tasks, and one rollout may contain several model generations chained through
those summaries. The report assigns the final task reward to all model-produced tokens in
the chain, so the pre- and post-summary generations share one terminal outcome signal
\citep{cursor2026composer2}. If the post-summary request no longer preserves the original
token prefix, Polar's provenance rule would represent the generations as separate unit
traces rather than reconstructing one false continuous sequence. This makes Composer 2
Polar-like at the trace-boundary level, although the report does not specify whether its
normalized advantage is computed once for the whole chain or separately for each chunk.

Figure~\ref{fig:computer-use-grpo-trace-chunks} illustrates the distinction in a
computer-use task. Screenshot pruning or context replacement makes naive whole-session
training condition later actions on observations the policy did not actually see. A
chunked construction preserves each model call's realized context while allowing a shared
terminal reward to credit the chain.

\begin{figure}[p]
  \centering
  \includegraphics[width=0.96\textwidth]{figures/PreviousGRPO.png}

  \vspace{4mm}

  \includegraphics[width=0.96\textwidth]{figures/ChunkGRPO_PolarLike.png}
  \caption{Conceptual computer-use comparison. Top: treating a context-managed session as
  one uninterrupted GRPO sequence can train on a prefix that was never observed by the
  acting policy. Bottom: a Polar-like construction splits the session at context changes,
  masks unavailable history, and retains the shared terminal outcome signal across
  provenance-valid chunks. The diagrams are explanatory illustrations, not figures from
  the cited reports.}
  \label{fig:computer-use-grpo-trace-chunks}
\end{figure}
